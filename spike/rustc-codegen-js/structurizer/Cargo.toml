[package]
name = "structurizer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dev-dependencies]
# Inline snapshot assertions for the golden CFG matrix (`UPDATE_EXPECT=1` rebaselines).
expect-test = "1.5.1"
